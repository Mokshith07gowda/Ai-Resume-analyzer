from fastapi import APIRouter
from backend.services.ats_service import calculate_ats

router = APIRouter(prefix="/ats", tags=["ATS Scoring"])

@router.post("/")
def ats_score(resume_data: dict):

    score = calculate_ats(resume_data)

    return score